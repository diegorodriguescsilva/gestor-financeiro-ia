version: "3.9"

services:
  app:
    build: .
    ports:
      - "5000:5000"
    environment:
      - ANTHROPIC_API_KEY=${ANTHROPIC_API_KEY}
    volumes:
      - db_data:/app/instance
    restart: unless-stopped

volumes:
  db_data:
